# Nurigo 개발 프로세스

## 1. 현재 상태

| 영역 | 상태 | 확인 내용 |
| --- | --- | --- |
| 모바일 사용자 구조 | 완료 | 하단 내비게이션과 지도·미션·마이 경로 구성 |
| 미션 프런트엔드 데모 | 완료 | 일일 4개 미션, 상세, 가이드, 도전 기록, 주간·월간 랭킹, 보상 상태 구현 |
| 사용자 지도 기본 기능 | 완료 | 네이버 지도, 시장 Polygon, 점포 조회·묶음·상세, 검색, 현재 위치 구현 |
| 관리자 지도 | 완료 | 시장 Polygon 생성과 점포 CSV 가져오기 구현 |
| 지도·미션 연결 | 완료 | ID 기반 데이터 결합, 미션 마커·상세 카드, 양방향 이동과 자동 포커스 구현 |
| 데모 시나리오 안정화 | 완료 | 선착순 미션, 보조 화면 진입, 지도 로딩·오류·빈 상태 피드백 구현 |
| 백엔드 실행 단위 미션 상태 | 완료 | 익명 세션별 개인 상태, 서버 공용 공동 미션, 실행 시 랜덤 구성 구현 |
| 미션 진행·보상 API | 완료 | 완료·중복 방지·보상 수령과 랭킹 포인트 반영 구현 |
| 비로그인 랭킹 | 완료 | 서버 실행 중 주간·월간 랭킹 상태 공유 및 유지 |
| 프런트엔드 미션 API 연동 | 완료 | 목록·완료·보상·랭킹과 로딩·오류 상태 연결 |
| 위치 판정 API | 대기 | 현재 완료 버튼이 위치 판정 결과를 재현 |

## 2. 개발 순서

### 단계 A. 지도·미션 연결 완성

1. 점포와 시장 데이터를 미션의 `storeId`, `marketId`로 결합한다.
2. 미션 대상 점포는 강조된 미션 마커로, 나머지 점포는 기존 일반 점포 마커로 표시한다.
3. 미션 마커를 선택하면 대상 장소 정보, 미션 요약, 상태, 보상을 함께 표시한다.
4. 지도에서 미션 상세로 이동하고, 미션 상세에서 돌아온 지도는 해당 미션 위치를 자동 선택한다.
5. 검색, 현재 위치, 시장 경계, 점포 묶음, 관리자 화면을 회귀 점검한다.

### 단계 B. 공모전 데모 시나리오 안정화

1. 시장 진입, 점포 방문, 체류, 연속 방문, 보상 수령 흐름을 하나의 시연 순서로 점검한다.
2. 데이터 없음, API 오류, 위치 권한 거부, 마감 미션 상태를 화면에 표현한다.
3. 모바일 화면 폭과 터치 영역, 키보드 접근성, 지도 오버레이 겹침을 점검한다.

### 단계 C. 백엔드 미션 도메인 구축

1. 코드 기반 미션 풀, 실행 단위 구성, 익명 세션 상태, 공동 미션과 기간별 랭킹 모델을 설계한다.
2. 개인 미션 2개와 우선 미션 2개 규칙, 중복 완료 방지, 공동 미션 참여 인원 처리를 구현한다.
3. 시장 Polygon 진입, 점포 반경 접근, 체류 시간, 지정 지점 순차 방문 판정을 구현한다.
4. 프런트엔드 데모 저장소를 API 저장소로 교체한다.

### 단계 D. 최종 검증

1. 프런트엔드 정적 검사와 프로덕션 빌드를 통과시킨다.
2. 백엔드 단위·통합 테스트를 실행한다.
3. 사용자 지도와 관리자 지도의 핵심 흐름을 수동 회귀 점검한다.
4. 변경 파일과 남은 제약을 정리해 감독 검토를 요청한다.

## 3. 서버 실행 상태 개발 단위의 완료 기준

- 회원가입과 로그인 없이 브라우저별 익명 세션 상태를 유지한다.
- 같은 브라우저의 새로고침은 미션 진행·보상 상태를 유지한다.
- 다른 브라우저와 시크릿 창은 서로 다른 개인 미션 상태를 가진다.
- 공동 미션의 전체 진행도와 남은 자리는 모든 익명 세션이 공유한다.
- 보상 수령 포인트는 서버 실행 중 공유 랭킹에 반영한다.
- 서버 재시작 시 미션 구성, 익명 세션, 공동 상태, 랭킹을 새로 생성한다.
- 지도·시장·상점 데이터만 기존 DB 영구 저장 구조를 유지한다.

## 4. 작업 원칙

- 기존 사용자·관리자 지도 기능을 보존한다.
- 화면 상태와 도메인 식별자는 좌표나 이름이 아니라 ID로 연결한다.
- 현재 데모 좌표는 API 데이터가 없을 때만 사용하는 대체값으로 둔다.
- 실제 미션 정책과 위치 판정은 프런트엔드에 고정하지 않는다.
- 커밋과 푸시는 감독 검토가 끝난 뒤 사용자가 직접 진행한다.

## 5. 이번 작업 검증 결과

- 프런트엔드 lint 통과
- 프런트엔드 프로덕션 build 통과
- 미션 정책·실행 상태·서비스·쿠키 단위 테스트 8개 통과
- 백엔드 전체 소스와 테스트 소스 컴파일 통과
- 390 × 844 모바일 화면에서 미션 마커와 상세 카드 배치 확인
- 시장 미션 2개 묶음과 점포 미션 2개 표시 확인
- `지도 → 미션 상세 → 대상 마커가 선택된 지도` 이동 확인
- 브라우저 콘솔 오류 없음

## 6. 백엔드 연동 원칙 및 다음 개발 항목

현재 범위에서는 회원가입, 로그인, 고정 데모 사용자를 구현하지 않는다. 백엔드가 발급한 세션 쿠키는 계정이 아니라 현재 서버 실행 중 개인 상태를 구분하는 임시 식별자다. 미션·공동 상태·랭킹은 메모리에만 두며 서버 재시작 시 초기화한다. 지도·시장·상점 데이터는 기존 DB에 유지한다.

다음 개발 항목은 아래 순서로 진행한다.

1. 시장 진입·점포 접근·체류 시간 위치 판정
2. 위치 판정 이벤트와 미션 진행·완료 API 연결
3. 도전 기록 화면을 실행 상태 API로 교체
4. 서버 중단·재시작과 공동 미션 다중 브라우저 수동 회귀 점검
5. 사용자 기능이 필요해지는 시점에 별도 설계 후 미션 도메인과 연결

미션·보상 규칙은 코드에 흩어지지 않도록 정책 계층으로 분리한다. 사용자 식별이나 서버 저장이 필요한 개발은 범위를 먼저 확인한 뒤 진행한다.
