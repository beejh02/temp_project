# Nurigo 개발 프로세스

## 1. 현재 상태

아래 완료 표는 기존 구현과 당시 검증 범위를 요약한다. 현재 최우선 목표는
공모전에서 위치 이동 → 미션 진행·완료 → 보상을 효율적으로 보여주는 것이다.
GPS/WASD와 미션 API는 연결되어 있으며, 발표 장소에서 쉽게 시작하고 결과를
보여주는 시연 흐름을 먼저 완성한다. 상세 순서는 `NEXT_DEVELOPMENT_PLAN.md` 6절을 따른다.

위치 시연 시작 기능은 구현·로컬 검증을 마쳤다. 사용자 지도에서 GPS 없이
미션 대상 남쪽에서 시작해 WASD로 이동할 수 있다. 다음 작업은 지도·미션 화면
왕복 시 시연 위치 유지와 진행 상태 표현이다.

| 영역 | 상태 | 확인 내용 |
| --- | --- | --- |
| 모바일 사용자 구조 | 완료 | 하단 내비게이션과 지도·미션·마이 경로 구성 |
| 미션 프런트엔드 데모 | 완료 | 일일 4개 미션, 상세, 가이드, 도전 기록, 주간·월간 랭킹, 보상 상태 구현 |
| 사용자 지도 기본 기능 | 완료 | HTTPS 기반 네이버 GL 지도, 시장 Polygon, 점포 조회·묶음·상세, 검색, 현재 위치, SDK 실패 재시도 구현 |
| 관리자 지도 | 완료 | 시장 Polygon 등록·선택·수정·삭제와 점포 CSV 가져오기 구현 |
| 시장 편집·미션 일관성 | 완료 | 미션 대상 시장 이름 변경·삭제 차단, 경계 수정 후 실행 카탈로그 재연결 구현 |
| 지도·미션 연결 | 완료 | ID 기반 데이터 결합, 미션 마커·상세 카드, 양방향 이동과 자동 포커스 구현 |
| 시장 미션 대상 연결 | 완료 | 서버 실행 시 DB 시장 ID를 결합하고 Polygon 내부 대표 좌표로 마커 표시 |
| 점포 미션 대상 연결 | 완료 | 서버 실행 시 대상 시장 내부 실제 점포를 중복 없이 선택하고 ID·이름·주소·좌표·30m 판정 연결 |
| 데모 시나리오 안정화 | 완료 | 선착순 미션, 보조 화면 진입, 지도 로딩·오류·빈 상태 피드백 구현 |
| 백엔드 실행 단위 미션 상태 | 완료 | 익명 세션별 개인 상태, 서버 공용 공동 미션, 실행 시 랜덤 구성 구현 |
| 미션 진행·보상 API | 완료 | 위치 이벤트 기반 완료·중복 방지·보상 수령과 랭킹 포인트 반영 구현 |
| 비로그인 랭킹 | 완료 | 서버 실행 중 주간·월간 랭킹 상태 공유 및 유지 |
| 프런트엔드 미션 API 연동 | 완료 | 목록·GPS/WASD 진행·보상·랭킹과 로딩·오류 상태 연결 |
| 위치 판정 API | 완료 | 시장 Polygon, 점포 반경 30m, 서로 다른 점포, 시장 체류 시간 판정 구현 |
| GPS 없는 위치 시연 | 구현·로컬 검증 완료 | API 미션 선택, 대상 남쪽 시작, WASD 판정과 시연 배지 구현 |
| 도전 기록 API | 완료 | 실행 단위 익명 세션의 3일 연속 방문 데모, 진행도·방문일·보상과 랭킹 반영 구현 |
| 공동 상태 자동 갱신 | 완료 | 화면 표시 중 미션·도전·랭킹 상태를 7.5초 간격으로 중복 요청 없이 갱신 |
| 위치 요청 신뢰성 | 완료 | 서버 수신 시간 기준 체류, GPS 50m 정확도, 오래된·미래·중복·역순 요청 방어 구현 |
| 프로토타입 실행 설정 | 완료 | Vite·Vercel API 프록시, Spring CORS origin, 익명 세션 쿠키 정책 검증 및 실행 문서화 |
| CI 자동 검증 | 완료 | GitHub Actions에서 프런트 test·lint·build와 독립 PostGIS 백엔드 전체 테스트 실행 |

## 2. 개발 순서

### 단계 A. 위치 기반 미션 연동 시연 완성 (구현·로컬 검증 완료)

1. 실제 GPS를 기다리지 않고 시연 시장 부근에서 시작할 수 있게 한다.
2. 기존 WASD와 위치 판정 API를 활용해 시장 진입·대상 점포 접근을 보여준다.
3. 위치 이동으로 대표 방문 미션이 완료되는 흐름을 연결한다.

### 단계 B. 지도·미션 상태 표현

1. 현재 위치, 대상 장소, 진행·완료 상태가 한눈에 보이도록 한다.
2. 지도 카드·미션 목록·상세에 같은 판정 결과를 반영한다.
3. 지도와 미션 화면을 왕복해도 시연 위치와 진행 흐름을 이어간다.

### 단계 C. 보상·랭킹 결과 연결

1. 완료 후 NP 수령과 지급 결과를 보여준다.
2. 획득 포인트가 기존 랭킹에 반영되는 흐름을 보여준다.
3. 점포 방문과 시장 탐색을 유도하는 서비스 목적을 시연 동선에 담는다.

### 단계 D. 발표 환경에서 반복 시연

1. 시연할 시장·점포·미션과 시작 상태를 준비한다.
2. 발표용 URL에서 위치 이동 → 완료 → 보상 → 랭킹을 확인한다.
3. 관련 테스트·기존 전체 검사를 수행하고 시연을 막는 문제만 수정한다.
4. 시작·조작·반복 방법과 확인 결과를 짧게 기록한다.

체류·연속 방문·공동 미션은 기존 화면의 보조 설명으로 활용할 수 있다.
긴 대기, 여러 날짜·브라우저 준비는 핵심 시연의 완료 조건에서 제외한다.

## 3. 서버 실행 상태 개발 단위의 완료 기준

- 회원가입과 로그인 없이 브라우저별 익명 세션 상태를 유지한다.
- 같은 브라우저의 새로고침은 미션 진행·보상 상태를 유지한다.
- 다른 브라우저와 시크릿 창은 서로 다른 개인 미션 상태를 가진다.
- 공동 미션의 전체 진행도와 남은 자리는 모든 익명 세션이 공유한다.
- 보상 수령 포인트는 서버 실행 중 공유 랭킹에 반영한다.
- 서버 재시작 시 미션 구성, 익명 세션, 공동 상태, 랭킹을 새로 생성한다.
- 지도·시장·상점 데이터만 기존 DB 영구 저장 구조를 유지한다.

## 4. 작업 원칙

- 기존 사용자·관리자 지도 기능을 보존한다.
- 개발 우선순위는 공모전에서 보여줄 사용자 경험으로 결정한다. 시연과 무관한
  날짜·위치 경계 조건이나 관리자 운영 예외를 독립 개발 과제로 추가하지 않는다.
- 화면 상태와 도메인 식별자는 좌표나 이름이 아니라 ID로 연결한다.
- 현재 데모 좌표는 API 데이터가 없을 때만 사용하는 대체값으로 둔다.
- 실제 미션 정책과 위치 판정은 프런트엔드에 고정하지 않는다.
- 변경은 `feat`, `fix`, `test`, `docs`, `chore`, `style` 등 기존 convention과 한국어 제목을 사용해 기능 단위로 커밋한다.
- 관련 검증이 끝나면 현재 저장소에 설정된 Git 사용자 정보를 유지한 채 CLI로 커밋하고 `origin/main`에 푸시한다.

## 5. 최근 작업 검증 결과

위치 시연 추가 후 프런트 테스트 41개·lint·build와 DB 연결이 없는 기존 백엔드
테스트 25개가 통과했다. 실제 네이버 지도·로컬 API fixture로 방문 완료와 보상
수령 화면, 390×844 배치를 확인했다. 실제 Render 통합 확인과 PostGIS 전체
로컬 검사는 수행하지 않았으며, 상세 검증 범위는 `NEXT_DEVELOPMENT_PLAN.md` 6.4절에 기록했다.

2026-09-05 재분석에서는 기준 커밋 `dc2a85c`의 프런트 테스트 37개·lint·build를
로컬에서 다시 통과했고, [CI #2](https://github.com/beejh02/temp_project/actions/runs/33894608452)의
프런트 및 독립 PostGIS 백엔드 전체 테스트 성공을 확인했다. 이번 로컬 백엔드
재실행은 Gradle 경로 오류와 테스트 DB 부재로 완료하지 않았다. 아래 상세 기록은
이전 작업의 검증 이력이며, 실제 배포·브라우저 재검증 결과와 구분한다.

- 프런트엔드 lint 통과
- 프런트엔드 컴포넌트·지도 SDK·실행 설정 테스트 37개 통과
- 프런트엔드 프로덕션 build 통과
- Vite API 대상 origin과 Vercel API rewrite 순서·HTTPS 주소 검증
- Spring CORS HTTP(S) origin과 익명 세션 쿠키 보안 조합 검증
- 미션 정책·위치·실행 상태·서비스·쿠키 단위 테스트 통과
- 독립 PostGIS 테스트 DB에서 백엔드 전체 테스트 44개 통과
- 테스트 전용 Flyway migration과 결정적 시장·점포 fixture 적용
- Hibernate 자동 DDL 생성을 중단하고 Flyway migration 검증 모드 적용
- 시장 체류 누적·이탈 후 재측정·점포 중복 제외·공동 미션 마감 후 위치 판정 검증
- 테스트 fixture의 대전 중앙시장과 Polygon 내부 대표 좌표가 시장 미션에 연결되는지 확인
- 테스트 fixture의 중앙시장 내부 점포 4개가 실행 단위 미션에 중복 없이 고정되는지 확인
- 대표 좌표에서 최초 방문·공동 방문·체류 미션 상태가 자동 갱신되는지 확인
- 동일 세션 상태 유지, 세션별 개인 상태 분리, 공동 상태 공유, 새 서버 실행 시 초기화 회귀 테스트 통과
- GPS로 받은 위치에서 WASD 이동 좌표가 미션 판정 API에 전달되는 흐름 확인
- 빠르게 연속 입력된 위치 판정 요청이 입력 순서대로 처리되는지 확인
- 위치 조회 실패 시 이전 좌표 전송을 멈추고 권한·재시도 안내를 표시하는지 확인
- 미션 갱신 실패 시 기존 목록 유지, 오류 안내, 수동 재시도 복구 흐름 확인
- 위치 판정 요청 실패 후에도 다음 위치 요청을 계속 처리하는지 확인
- 도전 기록·랭킹 조회 실패 후 수동 재시도와 빈 상태 표시 확인
- 도전 보상 실패 후 재시도 및 동일 미션 보상 요청 중복 방지 확인
- 390 × 844 화면에서 앱 조작 요소를 44px 이상으로 보정하고 하단 내비게이션과 겹치지 않는지 확인
- 지도 검색 입력의 접근 가능한 이름과 `검색 → 내 위치 → 하단 내비게이션` 키보드 포커스 순서 확인
- 주소 입력 중 WASD 키가 테스트 위치 이동을 실행하지 않는지 확인
- 390 × 844 모바일 화면에서 미션 마커와 상세 카드 배치 확인
- 시장 미션 2개 묶음과 점포 미션 2개 표시 확인
- `지도 → 미션 상세 → 대상 마커가 선택된 지도` 이동 확인
- 브라우저 콘솔 오류 없음
- CORS·익명 세션 쿠키·HTTP 계약 테스트 12개 통과
- GitHub Actions CI에서 프런트엔드와 PostGIS 백엔드 작업 모두 통과

## 6. 백엔드 연동 원칙 및 다음 개발 항목

현재 범위에서는 회원가입, 로그인, 고정 데모 사용자를 구현하지 않는다. 백엔드가 발급한 세션 쿠키는 계정이 아니라 현재 서버 실행 중 개인 상태를 구분하는 임시 식별자다. 미션·공동 상태·랭킹은 메모리에만 두며 서버 재시작 시 초기화한다. 지도·시장·상점 데이터는 기존 DB에 유지한다.

현재 애플리케이션 DB의 기준은 Supabase다. 로컬 개발 DB는 운영 데이터의
기준으로 사용하지 않으며, 자동 테스트는 별도 `temp_project_test` DB만
사용한다. 테스트 DB 구성과 실행 방법은 `backend/nurigo/README.md`를 따른다.
Supabase에 `flyway_schema_history`가 없다면 스키마 비교와 백업 없이 baseline을
적용하지 않는다.

다음 개발 순서는 2절의 공모전 시연 계획을 따른다. 위치 시연 시작 기능 다음으로
**지도·미션 화면 왕복 시 시연 위치 유지와 진행 상태 표현**을 진행한다.
자정 변경, 시장 안팎의 세밀한 판정, 관리자 경계 축소·CSV 재가져오기 동기화,
세션 응답 순서별 조합 검증은 현재 개발 계획에서 제외한다.
위치 기반 방문을 판정하는 기존 기본 기능은 유지하며, 시연을 막는 문제가
실제로 발생하면 해당 흐름 안에서 필요한 부분만 수정한다.

회원가입, 로그인, 관리자 인증, 사용자 상태 영속화와 다중 서버 대응은
현재 프로토타입 구현 범위에서 제외한다.

미션·보상 규칙은 코드에 흩어지지 않도록 정책 계층으로 분리한다. 사용자 식별이나 서버 저장이 필요한 개발은 범위를 먼저 확인한 뒤 진행한다.
