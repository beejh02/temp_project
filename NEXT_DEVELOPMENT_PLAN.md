# Nurigo 다음 개발 계획 (임시)

이 문서는 2026-09-05 재분석 기준 다음 개발 순서와 범위를 고정하기 위한 임시 작업 문서다.
각 단계가 완료되면 검증 결과를 기록하고, 전체 안정화 후 `DEVELOPMENT_PROCESS.md`에 병합한다.

기존 단계 1~5의 완료 기록은 유지한다. 현재 개발 목표는 공모전에서 위치 이동이
미션 완료와 보상으로 이어지는 흐름을 효율적으로 보여주는 것이다.
실제 착수 순서는 **6절의 공모전 시연 중심 개발 순서**를 따른다.
초기 아이디어 문서보다 이 문서의 고정 범위와 `DEVELOPMENT_PROCESS.md`를
현재 개발 기준으로 우선한다.

## 1. 고정 범위

### 진행하지 않는 기능

- 관리자 인증 및 관리자 계정
- 회원가입, 로그인, 사용자 계정
- 계정 기반 미션·보상·랭킹 영속화
- 마이페이지 상세 기능
- Supabase 스키마 변경, 신규 SQL migration, 별도 DB 인프라 개발

현재 익명 세션과 서버 실행 단위 메모리 상태를 유지한다. 위 항목은 사용자가 명시적으로 범위를 다시 변경하기 전까지 개발하지 않는다.

### 유지할 기능

- 사용자 지도와 관리자 지도
- 시장 Polygon 등록·조회·수정·삭제
- 점포 CSV 가져오기와 시장 내부 점포 조회
- 익명 세션 기반 미션 진행·보상·도전 기록·랭킹
- GPS 및 WASD 데모 위치 입력
- Vercel 프록시와 Render 백엔드 배포 구조

## 2. 2026-09-04 기준선

- `2e74cbe`: 시장 수정·삭제 API와 오류 응답 추가
- `c1853ff`: 관리자 지도에서 저장된 시장 선택·수정·삭제 흐름 추가
- `47d2b6a`: 관리자 시장 편집 컴포넌트·페이지·좌표 변환 테스트 추가
- `e4010a8`: 운영 DB와 분리된 PostGIS 테스트 프로필·fixture 추가
- `f88ffd7`: Hibernate 자동 DDL을 중단하고 Flyway 검증 모드 적용
- `b866efe`: 프런트 지도 테스트 환경변수 고정
- `7773cd9`: Supabase 운영 스키마와 테스트 DB 원칙 문서화
- 프런트엔드 테스트 20개, lint, 프로덕션 build 통과
- 시장 API 단위·HTTP 계약 테스트 통과
- pull된 기준에서는 독립 PostGIS 테스트 DB의 백엔드 전체 테스트 44개 통과

## 3. 상세 개발 순서

### 단계 1. 시장 편집 기능의 애플리케이션 검증 (완료)

목표는 기존 Supabase 연결과 스키마를 변경하지 않고 시장 수정·삭제 흐름을 안정화하는 것이다.

1. 시장 이름과 Polygon 입력을 애플리케이션 계층에서 검증한다.
2. 존재하지 않는 ID, 빈 이름, 잘못된 GeoJSON 타입, 닫히지 않은 ring, 유효하지 않은 Polygon의 응답을 검증한다.
3. 관리자 화면의 등록 → 선택 → 수정 → 목록 갱신 → 삭제 흐름을 검증한다.
4. API 오류 메시지가 관리자 화면에 그대로 전달되는지 확인한다.
5. 기존 Supabase 연결 설정과 SQL 스키마는 변경하지 않는다.

완료 기준:

- 관리자 화면과 API에서 동일한 오류 메시지를 확인할 수 있다.
- 프런트엔드 전체 테스트, lint, build와 백엔드 전체 테스트가 통과한다.

### 단계 2. 시장 변경과 실행 중 미션의 일관성 보장 (완료)

현재 `MissionRunCatalog`는 최초 조회 시 시장·점포 대상 ID, 이름, 대표 좌표를 메모리에 고정한다. 이후 관리자가 시장을 수정하거나 삭제하면 미션 응답의 위치와 실제 DB 경계가 달라질 수 있다.

1. 시장 수정이 미션 대상 이름·대표 좌표·점포 후보에 미치는 영향을 테스트로 재현한다.
2. 시장 삭제 시 해당 시장을 대상으로 하는 실행 중 미션이 수행 불가가 되는 상황을 재현한다.
3. 데모 실행의 단순성을 유지하면서 아래 정책을 적용한다.
   - 시장 수정 후 미션 카탈로그를 다시 해석한다.
   - 재해석 시 기존 익명 세션의 진행·보상 상태는 미션 키 기준으로 유지한다.
   - 미션 대상 시장을 삭제하려는 경우에는 삭제를 거부하고 이해 가능한 충돌 응답을 반환한다.
4. 관리자 화면에 재해석 결과 또는 삭제 거부 사유를 표시한다.
5. 사용자 지도와 미션 상세가 변경된 시장 위치를 즉시 사용하도록 검증한다.

완료 기준:

- 시장 수정 후 서버 재시작 없이 지도와 미션의 대상 정보가 일치한다.
- 미션 대상 시장은 실수로 삭제할 수 없다.
- 미션 진행 상태와 공동 미션 상태가 불필요하게 초기화되지 않는다.

### 단계 3. 지도·미션 전체 회귀 검증 (완료)

1. 서버 중단·재시작 시 익명 개인 상태와 공동 상태가 계획대로 초기화되는지 확인한다.
2. 일반 브라우저와 시크릿 창에서 개인 상태 분리와 공동 미션 공유를 확인한다.
3. GPS/WASD로 시장 최초 방문, 점포 접근, 서로 다른 점포 3곳, 시장 체류, 연속 방문 데모를 점검한다.
4. 완료·중복 방지·보상 수령·주간 및 월간 랭킹 반영을 확인한다.
5. 시장 편집 전후 사용자 지도 Polygon, 미션 마커, 상세 카드, 자동 포커스를 확인한다.
6. 위치 권한 거부, 낮은 정확도, 오래된·미래·중복·역순 위치 요청을 확인한다.
7. 모바일 390 × 844 화면과 키보드 접근성, 지도 오버레이 겹침을 점검한다.

완료 기준:

- 핵심 시연 흐름을 새 세션에서 반복 재현할 수 있다.
- 브라우저 콘솔 오류와 처리되지 않은 API 오류가 없다.
- 기존 사용자·관리자 지도 기능에 회귀가 없다.

### 단계 4. 프로토타입 실행 안정성 (완료)

1. 개발·테스트·운영 설정 파일과 환경변수 책임을 명확히 분리한다.
2. Vercel `/api/*` 프록시, Render CORS, 익명 세션 쿠키 설정을 자동 테스트한다.
3. 잘못된 API 대상과 CORS origin을 애플리케이션 시작 단계에서 거부한다.
4. 로컬·Vercel 프록시·Render 직접 호출별 설정과 오류 진단 절차를 문서화한다.
5. `README.md`와 `DEVELOPMENT_PROCESS.md`를 실제 실행·검증 결과에 맞게 갱신한다.

완료 기준:

- 새 환경에서도 문서만으로 로컬 실행과 검증이 가능하다.
- 잘못된 프록시·CORS·쿠키 설정이 실행 전에 자동 검증된다.

### 단계 5. 자동 검증 정리 (완료)

1. CI에서 프런트엔드 테스트·lint·build를 실행한다.
2. 독립 PostGIS 테스트 DB에서 백엔드 전체 테스트를 실행한다.
3. DB 연결 실패, 잘못된 환경변수, 마이그레이션 실패가 배포 전에 드러나도록 한다.

완료 기준:

- 커밋과 pull request에서 핵심 회귀를 자동으로 차단한다.
- 테스트 DB만 사용하며 Supabase 운영 데이터와 스키마를 변경하지 않는다.

### 단계 6. 배포 및 최종 시연 검증

먼저 6절의 위치 기반 미션 시연 흐름을 완성한 뒤 발표에 사용할 배포 환경에서 확인한다.

1. 시연 위치 시작 → 시장·점포 이동 → 미션 완료 → 보상 → 랭킹 순서를 준비한다.
2. 발표용 URL에서 같은 흐름을 실행하고 화면 전환과 상태 반영을 확인한다.
3. 시연을 실제로 막는 문제만 수정하고 결과를 기록한다.

완료 기준:

- 실제 시장에 가지 않아도 발표 장소에서 위치 기반 미션을 시연할 수 있다.
- 짧은 조작으로 방문·미션 완료·보상의 연결을 반복해서 보여줄 수 있다.

## 4. 작업 단위 공통 원칙

각 단계는 다음 순서로 진행한다.

1. 이번에 보여줄 시연 결과를 정하고, 그 흐름에서 실패하거나 빠진 동작을 재현한다.
2. 최소 범위로 구현한다.
3. 관련 테스트를 먼저 실행한다.
4. 프런트엔드 전체 테스트·lint·build와 백엔드 전체 테스트를 실행한다.
5. 수동 확인이 필요한 지도·브라우저 흐름을 점검한다.
6. 변경 내용, 검증 결과, 남은 제약을 문서에 기록한다.
7. 변경을 기능 단위로 나누고 기존 convention과 한국어 제목으로 커밋한다.
8. 현재 저장소의 Git 사용자 정보를 유지해 CLI로 커밋하고 `origin/main`에 푸시한다.

사용자가 요청하지 않은 인증·계정 기능을 해결책으로 도입하지 않는다.
새 테스트와 수정은 핵심 시연에 필요한 범위로 정한다. 문서만 바뀐 작업은 문서
정합성과 diff를 확인하며, 시연과 무관한 예외 시나리오를 별도 개발 항목으로 늘리지 않는다.

## 5. 진행 기록

### 2026-09-04: 단계 1 시작

- 시장 Polygon 변환 시 자기 교차, 면적 없음, 좌표 범위 초과를 거부하도록 검증을 추가했다.
- 닫히지 않은 GeoJSON ring은 기존처럼 서버에서 안전하게 닫아 변환한다.
- `MarketGeometryMapperTest`, `MarketServiceUnitTest`, `MarketHttpContractTest`를 함께 실행해 통과했다.
- 프런트엔드 테스트 20개, lint, 프로덕션 build를 다시 실행해 통과했다.
- Supabase 스키마나 데이터를 변경하지 않고 애플리케이션 계층 테스트만 진행한다.

### 2026-09-04: pull 반영 후 범위 정정

- 독립 PostGIS 테스트 프로필과 Flyway `validate` 설정이 pull로 추가된 것을 확인했다.
- 해당 설정은 기존 기준으로만 유지하고 추가 SQL·migration 개발은 진행하지 않는다.
- 개발 범위를 애플리케이션 검증과 시장·미션 연결 안정화로 한정했다.

### 2026-09-04: 단계 2 완료

- 미션 대상 시장은 공백·대소문자를 제외한 이름을 유지하도록 하고 다른 이름으로 변경하지 못하게 했다.
- 미션 대상 시장 삭제는 409 응답과 명확한 오류 메시지로 차단했다.
- 미션 대상 시장의 경계를 수정하면 메모리 카탈로그를 무효화하고 다음 요청에서 최신 시장·점포 정보를 다시 연결하도록 했다.
- 실행 상태 저장소는 건드리지 않아 기존 익명 세션의 진행·보상·공동 미션 상태를 미션 키 기준으로 유지한다.
- 관리자 화면에서 삭제 거부 메시지가 표시되는 흐름을 프런트엔드 테스트로 검증했다.
- 다음 작업은 서버 재시작과 다중 브라우저에서 익명 개인 상태 분리·공동 상태 공유를 회귀 검증하는 것이다.

### 2026-09-04: 서버 재시작·다중 브라우저 회귀 검증 완료

- 같은 익명 세션 ID를 다시 사용하면 배정 미션과 진행 상태가 유지되는지 확인했다.
- 서로 다른 세션은 개인 미션 상태를 공유하지 않는지 확인했다.
- 공동 미션 진행도는 서로 다른 세션에 동일하게 반영되는지 확인했다.
- 새로운 `MissionRunStateStore` 실행에서는 이전 세션과 공동 상태가 초기화되는지 확인했다.
- 신규 세션에만 쿠키를 발급하고 기존 쿠키를 재사용하는 HTTP 흐름을 확인했다.
- 관련 상태 저장소·서비스·컨트롤러·쿠키 테스트 묶음이 모두 통과했다.
- 다음 작업은 GPS/WASD 핵심 미션 시나리오를 하나의 통합 흐름으로 검증하는 것이다.

### 2026-09-04: GPS/WASD 핵심 미션 흐름 검증 완료

- 백엔드의 시장 진입, 점포 반경 접근, 서로 다른 점포 방문, 체류, 연속 방문, 완료·중복 방지·보상·랭킹 시나리오를 회귀 검증했다.
- GPS로 받은 현재 위치에서 WASD로 이동한 좌표와 측정 시각이 미션 판정 요청에 전달되는지 프런트엔드 테스트로 확인했다.
- 위치 입력이 빠르게 연속 발생해도 이전 판정 완료 후 다음 요청을 입력 순서대로 처리하는지 확인했다.
- 프런트엔드 테스트 23개, lint, 프로덕션 build와 관련 백엔드 테스트가 모두 통과했다.
- 다음 작업은 위치 권한 거부와 API 실패를 포함한 프런트엔드 사용성·예외 처리를 보강하는 것이다.

### 2026-09-04: 미션 API 오류 복구 완료

- 백그라운드 미션 갱신에 실패해도 마지막으로 확인한 목록을 유지하도록 기존 동작을 회귀 검증했다.
- 미션 목록 위에 비차단 오류 안내와 `다시 시도` 버튼을 표시하도록 보강했다.
- 수동 재시도가 성공하면 오류 상태가 해제되고 최신 미션 상태로 복구되는지 확인했다.
- 한 위치 판정 요청이 실패해도 대기 중인 다음 위치 요청을 계속 처리하는지 확인했다.
- 프런트엔드 테스트 28개, lint, 프로덕션 build가 모두 통과했다.
- 다음 작업 후보는 도전 기록·랭킹·보상 수령 화면의 예외 처리이며, 사용자 확인 전에는 시작하지 않는다.

### 2026-09-04: 미션 보조 화면 예외 처리 완료

- 도전 기록과 랭킹의 최초 조회 실패 화면에 수동 재시도 동작을 추가했다.
- 도전 기록이 없거나 랭킹 참여자가 없을 때 명시적인 빈 상태를 표시하도록 했다.
- 백그라운드 재조회가 연속 실패해도 마지막으로 확인한 도전·랭킹 데이터를 유지한다.
- 도전 보상 실패 후 버튼을 다시 활성화해 재시도할 수 있는지 확인했다.
- 동일 미션의 보상 수령 요청이 처리 중이면 같은 요청을 다시 전송하지 않도록 했다.
- 프런트엔드 테스트 32개, lint, 프로덕션 build가 모두 통과했다.
- 다음 작업 후보는 390×844 화면 기준 모바일 사용성과 키보드 접근성 점검이며, 사용자 확인 전에는 시작하지 않는다.

### 2026-09-05: 모바일 사용성과 키보드 접근성 점검 완료

- 실제 390×844 브라우저에서 지도·미션·도전 기록·랭킹 화면을 확인했다.
- 검색, 현재 위치, 지도 데이터 재시도, 미션 재시도, 랭킹 탭, 뒤로가기와 점포 마커 등 앱 조작 영역을 최소 44px로 보정했다.
- 지도 오버레이와 하단 내비게이션이 겹치지 않고 오류 카드와 보조 화면이 화면 폭 안에 표시되는지 확인했다.
- 지도 검색 입력에 접근 가능한 이름과 상태 알림 역할을 추가했다.
- 키보드 포커스가 검색 입력에서 검색 버튼, 현재 위치, 하단 내비게이션 순서로 이동하는지 확인했다.
- 입력 요소 사용 중 WASD 테스트 이동이 실행되지 않는 회귀 테스트를 추가했다.
- 프런트엔드 테스트 34개, lint, 프로덕션 build가 모두 통과했다.
- 다음 작업 후보는 환경변수·API 프록시·CORS·익명 세션 쿠키를 포함한 프로토타입 실행 안정성 점검이며, 사용자 확인 전에는 시작하지 않는다.

### 2026-09-05: 프로토타입 실행 안정성 점검 완료

- `VITE_API_TARGET`이 경로 없는 HTTP(S) origin인지 Vite 시작 전에 검증하도록 했다.
- Vercel `/api/*` rewrite가 HTTPS Render 백엔드를 가리키고 SPA fallback보다 먼저 적용되는지 자동 테스트를 추가했다.
- Spring CORS 설정에서 빈 값, 와일드카드, HTTP(S)가 아닌 주소, 인증 정보, 경로, query를 거부하도록 했다.
- Vercel 프록시와 Render 직접 호출에 맞는 CORS·익명 세션 쿠키 설정 및 오류 진단 절차를 프런트엔드·백엔드 README에 기록했다.
- 프런트엔드 테스트 37개, lint, 프로덕션 build와 CORS·쿠키·HTTP 계약 테스트 12개가 통과했다.
- 다음 작업 후보는 프런트엔드와 독립 PostGIS 백엔드 테스트를 CI에 연결하는 자동 검증 정리이며, 사용자 확인 전에는 시작하지 않는다.

### 2026-09-05: 자동 검증 정리 완료

- `push`, `pull_request`, 수동 실행에서 동작하는 GitHub Actions CI를 추가했다.
- 프런트엔드는 Node.js 24와 lockfile 기반 `npm ci` 환경에서 테스트 37개, lint, 프로덕션 build를 실행한다.
- 백엔드는 Java 17과 임시 PostGIS 17 서비스를 사용해 기존 Flyway migration과 전체 테스트를 실행한다.
- CI에는 `TEST_DB_*`만 고정해 운영 `DB_*`와 Supabase 비밀값을 전달하지 않는다.
- 최초 CI 실행에서 프런트엔드와 백엔드 작업이 모두 통과했다.
- 다음 작업 후보는 실제 Vercel·Render 배포와 핵심 시연 흐름을 확인하는 배포 및 최종 시연 검증이며, 사용자 확인 전에는 시작하지 않는다.

## 6. 2026-09-05 공모전 시연 중심 개발 순서

### 6.1 현재 구현과 검증 기준

- 분석 기준 커밋은 `dc2a85c`이며, 분석 시작 시 `main` 작업 트리는 깨끗했다.
- React 19·Vite 프런트엔드와 Java 17·Spring Boot 백엔드가 연결되어 있다.
  시장·점포는 기존 PostGIS DB에 저장하고, 미션·보상·도전·랭킹은 익명 세션과
  서버 실행 단위 메모리 상태로 유지한다.
- 사용자·관리자 지도, 시장 CRUD, 점포 CSV, 미션 진행·보상·도전·랭킹,
  GPS/WASD, 모바일 접근성 및 오류 복구가 구현되어 있다.
- 로컬에서 프런트엔드 테스트 **37개**, lint, 프로덕션 build를 다시 실행해 통과했다.
  기본 `npm` 실행기의 경로 오류가 있어 설치된 Node.js의 `npm-cli.js`를 직접 사용했다.
- 기준 커밋의 [GitHub Actions CI #2](https://github.com/beejh02/temp_project/actions/runs/33894608452)에서
  프런트엔드와 독립 PostGIS 백엔드 전체 테스트 작업이 모두 성공한 것을 직접 확인했다.
- 이번 로컬 백엔드 실행은 Gradle 캐시 잠금 디렉터리 오류로 테스트 시작 전에 멈췄다.
  Docker 명령과 5434 포트의 테스트 DB도 확인되지 않아 로컬 전체 테스트는 재실행하지 않았다.
  백엔드 통과 근거는 위 CI 결과이며, 운영 Supabase를 대체 테스트 DB로 사용하지 않았다.
- 같은 커밋의 GitHub 상태에 Vercel 성공이 기록되어 있다. Render의 배포 커밋,
  실제 HTTP 응답, 브라우저 쿠키 유지 및 최종 시연은 이번 분석에서 검증하지 않았다.

### 6.2 공모전 시연 범위

개발의 판단 기준은 **짧은 시연에서 위치 이동이 시장 방문·미션 완료·보상으로
이어지는 모습을 보여줄 수 있는가**다. 현재 구현을 활용해 이 흐름부터 완성한다.

기본 시연은 시장·점포 방문과 보상에 집중한다. 시장·점포 데이터는 시연 전에
준비하고, 발표 도중 경계를 수정하거나 CSV를 다시 가져오는 과정을 요구하지 않는다.
10분 체류, 연속 방문, 공동 미션은 기존 화면을 보조 설명에 활용할 수 있으며,
장시간 대기나 여러 날짜·브라우저 준비를 핵심 시연의 완료 조건으로 삼지 않는다.

다음 항목은 현재 개발 계획에서 제거한다.

- 자정·날짜 변경, 시장 안팎 점포의 세밀한 경계 조건 검증
- 관리자 경계 축소·CSV 재가져오기 후 실시간 동기화 보강
- 최초 세션 응답 순서 조합, 운영 장애·재시작 경우의 수를 확장하는 별도 검증
- 시연과 직접 관련 없는 구조 정리 및 배포·DB 운영 점검 확대

이는 추가 개발 범위에 대한 기준이다. 기존 위치 판정과 보상 기능을 활용하며,
핵심 시연을 실제로 막는 문제는 해당 작업 안에서 필요한 부분만 수정한다.

### 6-A. 위치 기반 미션 연동을 시연 가능한 흐름으로 완성 (구현·로컬 검증 완료)

현재 연결은 다음과 같다.

- `CurrentLocation`의 GPS/WASD 좌표 → `MissionDemoProvider`
- `POST /api/missions/location` → 서버의 시장 진입·점포 접근 판정
- 응답의 미션 진행·완료 상태 → 프런트엔드 미션 상태 갱신

기존 WASD 이동은 실제 GPS를 받아야 시작할 수 있었다. 사용자 지도에
`위치 시연 → 미션 선택 → 시연 시작`을 추가해 GPS 없이 시작할 수 있게 했다.
점포 미션은 대상 약 67m 남쪽, 시장 미션은 API Polygon의 남쪽 바깥에서 시작한다.

1. 실제 GPS를 기다리지 않고 시연 시장 부근에서 위치 이동을 시작할 수 있게 한다.
   시연 좌표는 API의 대상 시장·점포와 연결하고, 화면에서 시연 위치임을 표시한다.
2. 기존 WASD 이동을 같은 위치 판정 API에 연결해 시장 진입과 대상 점포 접근을
   보여준다. 실제 GPS로 받은 위치도 같은 진행·완료 흐름을 사용한다.
3. 배정된 대표 방문 미션에 맞춰 이동 경로를 준비하고, 위치 변화로 미션이
   진행·완료되는 것을 확인한다.

완료 기준: 발표 장소에서 시연을 시작하고, 대상 장소로 이동하는 조작만으로
방문 미션 완료까지 보여줄 수 있다.

### 6-B. 지도에서 미션 변화가 잘 보이도록 연결 (구현·로컬 검증 완료)

1. 지도에서 현재 위치와 미션 대상 장소를 알아보기 쉽게 표시한다.
2. 위치 판정 후 진행·완료 변화를 지도 카드·미션 목록·상세 화면에 반영한다.
3. 지도와 미션 상세를 왕복할 때 시연 위치와 진행 흐름이 끊기지 않도록 한다.
4. 이동 도중 시점을 되돌리는 동작 등 핵심 시연에서 실제로 불편한 부분만 보강한다.

완료 기준: 설명을 길게 하지 않아도 어디를 방문했고 어떤 미션이 완료됐는지
화면으로 이해할 수 있다.

### 6-C. 완료에서 보상·랭킹까지 이어지는 결과 표현

1. 완료한 미션의 NP 보상 수령과 지급 결과를 보여준다.
2. 받은 포인트가 기존 랭킹에 반영되는 것을 연결한다.
3. 대표 방문 미션의 목적이 점포 방문·시장 탐색 유도라는 점을 기존 설명과
   시연 동선에서 전달한다.

완료 기준: 방문 → 완료 → 보상 → 참여 순위를 한 흐름으로 보여줄 수 있다.
새 보상 정책이나 랭킹 집계 기능은 추가하지 않는다.

### 6-D. 발표 환경에서 짧게 반복 시연

1. 시연할 시장·점포·대표 미션과 시작 상태를 준비한다.
2. 발표용 URL에서 위치 시작 → 이동 → 완료 → 보상 → 랭킹을 실행한다.
3. 실제 발표 화면 크기에서 버튼과 상태 표시를 확인하고, 시연을 막는 오류만 수정한다.
4. 시작 방법, 조작 순서, 다시 시연하는 방법을 짧게 기록한다.

완료 기준: 실제 시장 방문이나 긴 대기 없이 핵심 기능을 반복해서 보여줄 수 있다.
배포 점검도 이 시연 흐름이 정상 실행되는 데 필요한 범위로 한정한다.

### 6.3 기존 작업방식 적용

첫 개발 단위는 **시연 시작 위치 → WASD 이동 → 위치 API 판정 → 방문 미션 완료**다.
이번 문서 수정으로 앞선 시장 편집·경계 검증 우선 계획을 대체한다.

작업 방식은 시연 시나리오 확인 → 필요한 구현 → 관련·기존 전체 검사 →
핵심 시연 확인 → 결과 기록 → 한국어 convention 커밋·푸시를 유지한다.
새 검증 항목은 시연에 필요한 만큼만 추가하며, 현재 구현과 앞으로 할 작업을 구분한다.
한 커밋에는 한 기능과 관련 테스트만 포함하고, 다른 기능과 문서 변경은 별도
커밋으로 나눈다. 여러 기능을 큰 커밋 하나로 묶지 않는다.

### 6.4 위치 시연 시작 기능 개발 결과

- 사용자 지도에서 배정된 미션을 선택하고 GPS 권한 없이 시연 위치를 생성한다.
- 기존 WASD·위치 요청 API·서버 완료 응답을 활용한다. 시연 시작 자체가 미션을
  완료 처리하지 않으며, 서버 응답을 받은 후 화면에 완료 상태를 반영한다.
- 시연 위치 배지와 조작 안내를 표시하고, 시작 후 패널을 접어 지도를 확보한다.
- GPS 추적 중에도 시연으로 전환할 수 있으며, 지연된 GPS 응답이 시연 위치를
  덮어쓰지 않는다. 관리자 지도는 기존 내 위치 기능을 유지한다.
- 프런트 테스트 41개·lint·build 통과. 사용자 지도·Provider·상세 화면을 연결한
  테스트에서 위치 요청 → 서버 완료 응답 → 보상 수령 화면을 확인했다.
- 기존 Gradle 캐시 경로를 지정해 DB 연결이 없는 백엔드 위치·미션·보상·세션
  테스트 25개를 오프라인으로 실행해 통과했다. 독립 PostGIS 전체 검사는 이번
  로컬 환경에 테스트 DB가 없어 실행하지 않았다.
- 실제 네이버 지도와 로컬 API fixture로 시연 시작 → WASD 접근 → 카드 완료 →
  상세 보상 수령을 브라우저에서 확인하고, 390×844 화면에서 패널 배치를 확인했다.
  fixture 응답을 사용한 화면 검증이며, 실제 Render와 연결한 통합 검증은 아니다.
- 실제 Render 연결 브라우저 검증은 쿠키·요청 정보의 외부 전송에 대한 자동 승인
  검토가 거부해 중단했다. 외부 미션 API를 호출하지 않는 로컬 검증으로 대체했다.
- `f29b101`을 `origin/main`에 반영한 뒤 [CI 실행 결과](https://github.com/beejh02/temp_project/actions/runs/33955032502)에서
  프런트 전체 검사와 독립 PostGIS 백엔드 전체 테스트가 모두 통과한 것을 확인했다.

당시 다음 작업이었던 6-B의 구현 결과는 아래 6.5절에 기록한다.
현재 `시연 시작`은 위치만 다시 배치하며 미션 기록을 초기화하지 않는다.

### 6.5 지도·미션 시연 연속성 개발 결과

- `adea788`: 시연 좌표·대상·이동 속도를 사용자 화면의 메모리에 보관한다.
  지도·미션 상세 왕복과 보상 수령 후에도 같은 위치에서 이동을 이어간다.
  지도 밖에서는 위치를 주기적으로 보내지 않으며, `내 위치` 전환과 새로고침은
  시연 위치 보관을 해제한다. 미션·보상 기록의 기존 서버 세션 정책은 유지한다.
- `0f679bc`: 미션 대상 ID·좌표가 바뀔 때만 자동으로 지도 시점을 이동한다.
  주기적 조회나 위치 판정으로 상태가 바뀌면 카드 내용만 갱신한다.
- 두 수정은 각각 기능·관련 테스트만 묶어 3개 파일, 2개 파일의 별도 커밋으로 만들었다.
- 프런트 전체 테스트 42개·lint·build 통과. 상세 왕복 후 위치·속도 복원과
  GPS 전환 시 복원 해제, 상태 갱신 시 카드 변경·지도 시점 유지가 검증됐다.
- 실제 네이버 지도와 로컬 API fixture에서 시연 시작 → 상세 왕복 → 같은 속도로
  이동 → 방문 완료를 확인했다. 지도를 옮겨둔 상태에서도 완료 카드 갱신 전후
  미션 마커의 화면 위치가 유지됐다. 실제 Render API 통합 검증은 포함하지 않는다.

다음 개발은 6-C의 보상·랭킹 결과 연결이다. 기존 보상과 랭킹 API를 활용해
방문 완료부터 포인트·참여 순위 확인까지의 화면 동선을 정리한다.
