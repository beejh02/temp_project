/*
 * 향후 점포 기반 미션을 표시하는 레이어입니다.
 * UserMapPage에서 선택된 Store 도메인 객체를 이 컴포넌트로 전달하므로,
 * 미션은 좌표나 상호명 대신 안정적인 store.id를 기준으로 연결할 수 있습니다.
 */
function MissionLayer() {
  return null;
}

export default MissionLayer;
